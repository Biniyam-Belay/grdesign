import { generateMeta } from "@lib/meta";

export const metadata = generateMeta({
  title: "Services",
  description:
    "Design services including brand identity, editorial design, packaging, and digital design.",
});
